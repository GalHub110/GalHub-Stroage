-- [[ CONFIGURATION OWNER ]] --
_G.MyID = 12345678 -- << GANTI DENGAN USER ID ROBLOX KAMU
_G.LogoID = "rbxassetid://1001397244" -- ID Logo GH Kamu

-- ==========================================
--        1. INTRO ANIMATION GALHUB
-- ==========================================
local TweenService = game:GetService("TweenService")
local IntroGui = Instance.new("ScreenGui", game:GetService("CoreGui"))
local Bg = Instance.new("Frame", IntroGui)
Bg.Size, Bg.BackgroundColor3 = UDim2.new(1,0,1,0), Color3.new(0,0,0)

local Txt = Instance.new("TextLabel", Bg)
Txt.Text, Txt.Font, Txt.TextScaled = "GALHUB", Enum.Font.FredokaOne, true
Txt.Size, Txt.Position, Txt.TextColor3 = UDim2.new(0,600,0,250), UDim2.new(0.5,-300,0.5,-125), Color3.new(1,1,1)
Txt.BackgroundTransparency, Txt.TextTransparency = 1, 1

task.wait(0.5)
Txt.TextTransparency = 0
Txt:TweenSizeAndPosition(UDim2.new(0,250,0,100), UDim2.new(0.5,-125,0.5,-50), "Out", "Quad", 1.5)
task.wait(1.6)
for i=1,3 do Txt.Visible = false; task.wait(0.1); Txt.Visible = true; task.wait(0.1) end
task.wait(0.5)
TweenService:Create(Bg, TweenInfo.new(1), {BackgroundTransparency = 1}):Play()
TweenService:Create(Txt, TweenInfo.new(0.5), {TextTransparency = 1}):Play()
task.wait(1.2); IntroGui:Destroy()

-- ==========================================
--        2. OVERHEAD TITLE SYSTEM
-- ==========================================
local function CreateOwnerTitle(plr)
    local char = plr.Character or plr.CharacterAdded:Wait()
    local head = char:WaitForChild("Head")
    if head:FindFirstChild("OwnerTitle") then head.OwnerTitle:Destroy() end
    
    local bbg = Instance.new("BillboardGui", head)
    bbg.Name = "OwnerTitle"; bbg.Size = UDim2.new(0,250,0,50); bbg.AlwaysOnTop = true; bbg.ExtentsOffset = Vector3.new(0,3,0)
    
    local lbl = Instance.new("TextLabel", bbg)
    lbl.Size = UDim2.new(1,0,1,0); lbl.BackgroundTransparency = 1; lbl.TextStrokeTransparency = 0
    lbl.Font = Enum.Font.FredokaOne; lbl.TextSize = 25
    
    local sensor = string.rep("*", #plr.Name)
    lbl.Text = "🔥👑O W N E R ["..sensor.."]"
    
    spawn(function()
        while task.wait(0.1) do lbl.TextColor3 = Color3.fromHSV(tick() % 5 / 5, 1, 1) end
    end)
end

if game.Players.LocalPlayer.UserId == _G.MyID then
    CreateOwnerTitle(game.Players.LocalPlayer)
    game.Players.LocalPlayer.CharacterAdded:Connect(function() task.wait(1); CreateOwnerTitle(game.Players.LocalPlayer) end)
end

-- ==========================================
--        3. MAIN UI & TABS (GALHUB ENGINE)
-- ==========================================
local Lib = loadstring(game:HttpGet("https://raw.githubusercontent.com/GrexXMeng/Mengs/refs/heads/main/library"))()
local Win = Lib:CreateWindow("GalHub", "v1.0.0", Enum.KeyCode.RightControl)

-- Floating Icon GH
local Screen = Instance.new("ScreenGui", game:GetService("CoreGui"))
local Icon = Instance.new("ImageButton", Screen)
Icon.Size, Icon.Image, Icon.Draggable = UDim2.new(0,65,0,65), _G.LogoID, true
Icon.Position, Icon.BackgroundTransparency = UDim2.new(0.05,0,0.4,0), 1
Icon.MouseButton1Click:Connect(function() Win:Toggle() end)

-- TAB MAIN
local Main = Win:CreateTab("MAIN")
Main:CreateToggle("Auto Unlock Island/Mission", function(s) _G.Unlock = s end)
Main:CreateToggle("Auto Cast & Shake", function(s) _G.AutoF = s end)
Main:CreateDropdown("Spawn Boat", {"Motorboat", "Sloop", "Longship", "Luxury Yacht"}, function(v) end)

-- TAB SHOP
local Shop = Win:CreateTab("SHOP")
Shop:CreateButton("TP to Daily Merchant", function() end)
Shop:CreateToggle("Auto Buy Daily Items", function(s) _G.ABuy = s end)
Shop:CreateInput("Custom Buy Rod", "Mythical Rod", function(t) _G.TRod = t end)

-- TAB SELL
local Sell = Win:CreateTab("SELL")
Sell:CreateLabel("--- FAVORITE RARITY ---")
local rr = {"Trash","Common","Rare","Epic","Legendary","Mythic","Exotic","Secret"}
for _, r in pairs(rr) do Sell:CreateToggle("Fav "..r, function(s) _G["Fav"..r] = s end) end
Sell:CreateLabel("--- AUTO SELL ---")
Sell:CreateToggle("Auto Sell Rarity", function(s) _G.ASell = s end)

-- TAB AFK
local Afk = Win:CreateTab("AFK")
Afk:CreateToggle("HUNT MYTHIC/SECRET ONLY", function(s) _G.HighOnly = s end)
Afk:CreateToggle("Auto Hunt Event Boss", function(s) _G.AHunt = s end)
Afk:CreateToggle("Instant Shake & Reel", function(s) _G.IShake = s end)

-- TAB MISCH
local Misc = Win:CreateTab("MISCH")
Misc:CreateSlider("Walkspeed", 16, 300, 16, function(v) game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = v end)
Misc:CreateToggle("Freeze Player", function(s) game.Players.LocalPlayer.Character.HumanoidRootPart.Anchored = s end)

-- TAB CONFIG (KEY SYSTEM)
local Cfg = Win:CreateTab("CONFIG")
local Keys = {["OWNERGACOR"]=true, ["GALHUB"]=true, ["NEWUPDT"]=true, ["CIHUYY"]=true}

Cfg:CreateInput("Enter Promo Code", "Ketik...", function(v) _G.KeyIn = v end)
Cfg:CreateButton("Redeem Premium", function()
    if Keys[_G.KeyIn] then
        Keys[_G.KeyIn] = nil -- One-time use per session
        _G.IsPremium = true
        CreateOwnerTitle(game.Players.LocalPlayer)
        Lib:Notify("SUCCESS", "Status: 🔥👑O W N E R Aktif!", 5)
    else
        Lib:Notify("FAILED", "Kode Salah atau Sudah Terpakai!", 3)
    end
end)

Cfg:CreateLabel("--- SECURITY STATUS ---")
if game.Players.LocalPlayer.UserId == _G.MyID then
    Cfg:CreateLabel("STATUS: 👑 OWNER (PREMIUM)")
else
    Cfg:CreateLabel("STATUS: 🆓 FREE USER")
end

Lib:Notify("GalHub Loaded", "Selamat Datang di Fisch Sultan Edition!", 5)
